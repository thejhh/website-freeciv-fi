p Pelin aloittamiseen tarvittava LUA-skripti:

pre
	- each player, i in work.players
		- if(player.number <= 126)
			| ; Player !{player.number}
			| lua create_player("!{player.username}", find.nation_type("!{player.nation}"))
			| aitoggle !{player.username}
